import { Card } from '@material-ui/core';
import { CloseOutlined } from '@material-ui/icons';

export function Home() {
  return (
    <div>
      <Card>
        <CloseOutlined />
      </Card>
    </div>
  );
}
